# 注意事项

## 工具
- `move_file` 不支持移动文件夹，仅支持移动单个文件
- `move_file` 不允许在移动时改变文件名（如需改名请先用 `rename_file`，再移动）
- `rename_file` 仅允许修改文件名，不允许改变文件所在目录（如需移动请用 `move_file`）
- `move_file` 的 `target_path` 可以是一个已存在的文件夹路径，文件会被移入该文件夹内；也可以是一个完整的目标文件路径

## 路径
- 所有路径必须以 `res://` 开头