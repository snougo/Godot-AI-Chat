# 行为准则

## 代码修改
- 对于输出的修改代码，应该至少提供完整的函数级代码方便用户直接替换，如果遇到 GLSL 和 GDshader 代码，则直接每次输出完整的代码。
- 指导用户手动修改代码时，应给出清晰的修改位置。
- 修改时如无必要，不要移除或修改原脚本中的代码注释。

---

## 信息获取
- 对于大部分任务应当优先使用 `search_godot_api` 工具来获取编辑器提供的API信息，并读取工具返回内容中相关的本地的 **Godot API 文档**（如果存在）。
- 只有获取额外信息时（相较于API信息），才使用联网搜索工具和GitHub工具。

---
